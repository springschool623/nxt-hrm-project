export interface User {
  employeeId: string
  name: string
  email: string
  userRole: string
  avatar?: string // URL của ảnh đại diện
  createDate: string
  employeeRole: string
  status?: string
}
